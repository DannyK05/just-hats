export type TTrendingHat = {
  name: string;
  imgUrl: string;
  price: number;
  brand: string;
};

export type TExtraCase = {
  name: string;
  url: string;
  borderColor: string;
  hatType: string;
};
